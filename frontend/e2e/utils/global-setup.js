/**
 * Global setup pour les tests E2E Playwright
 * Force la langue française par défaut pour tous les tests mock-only
 */
export default async function globalSetup() {
  // Cette fonction est exécutée une fois avant tous les tests
  // On peut y ajouter des configurations globales si nécessaire
  // Pour la langue, on utilise addInitScript dans chaque test via setupMockOnlyTest
}


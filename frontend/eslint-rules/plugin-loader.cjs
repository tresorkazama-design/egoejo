/**
 * Loader pour le plugin ESLint custom EGOEJO
 * 
 * Ce fichier charge le plugin de manière à éviter les problèmes de sérialisation
 * dans la configuration ESLint.
 */
module.exports = require('./index.cjs');

